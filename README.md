# lab-range-governance
Multi-tenant Lab Result Normal-Range Governance system: preserves reported reference ranges per result, standardizes tests/units, enables safe cross-lab trending, and generates audit-ready interoperability outputs (FHIR/HL7v2/CSV).
